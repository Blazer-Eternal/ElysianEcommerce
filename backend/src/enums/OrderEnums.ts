export enum OrderStatusEnum {
  pending = "pending",
  paid = "paid",
  shipped = "shipped",
  delivered = "delivered",
  cancelled = "cancelled",
}

export enum PaymentStatusEnum {
  unpaid = "unpaid",
  paid = "paid",
  refunded = "refunded",
}

export enum PaymentMethodEnum {
  cod = "cod",
  esewa = "esewa",
}